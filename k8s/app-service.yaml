apiVersion: v1
kind: Service
metadata:
  name: email-agent-service
  namespace: email-app
  labels:
    app: email-agent
spec:
  selector:
    app: email-agent
  ports:
    - port: 80
      targetPort: 7860
  type: NodePort
